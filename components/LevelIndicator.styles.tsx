import { StyleSheet } from "react-native"

export const levelIndicatorStyles = StyleSheet.create({
    container: {
        // backgroundColor: 'red',
        flexDirection: 'row',
        gap: 5,
        justifyContent: 'space-between',
    }
})